SELECT I.OCORRENCIAS,  I.NOME,  I.MATRICULAFUNCIONAL, R.ARQUIVOIMPORTACAO  
  FROM SAM_ROTINAIMP_OCORRENCIA I
  JOIN SAM_ROTINAIMP R ON (R.HANDLE = I.ROTINAIMP)
  WHERE  i.rotinaimp >= 447
    AND I.OCORRENCIAS  like '%CPF INVÁLIDO%'
  ORDER BY R.ARQUIVOIMPORTACAO, I.HANDLE